\documentclass[12pt]{article}
%#9f6efa
% ==========================
%   FONT + LANGUAGE SUPPORT
% ==========================
\usepackage{fontspec} % Required for XeLaTeX
\setmainfont[
    UprightFont = *-Regular,
    ItalicFont  = *-Italic,
    BoldFont    = *-Bold
]{Atkinson Hyperlegible}
\defaultfontfeatures{Ligatures=TeX}



% ==========================
%   PAGE GEOMETRY
% ==========================
\usepackage{geometry}
\geometry{
  a4paper,
  margin=2.5cm
}

% ==========================
%   PARAGRAPH + SPACING
% ==========================
\usepackage{setspace}
\onehalfspacing

\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

\usepackage{longtable,booktabs,array}
\usepackage{calc}
% ==========================
%   GRAPHICS
% ==========================
\usepackage{graphicx}
\graphicspath{{./}}

% ==========================
%   FIGURE NUMBERING
% ==========================
\usepackage{wrapfig}
\usepackage{caption}
\usepackage{float}
\usepackage{placeins}
\captionsetup[figure]{labelfont=bf}
\setlength{\intextsep}{2pt}        
\setlength{\columnsep}{5pt} 

% ==========================
%   LISTS (Pandoc fix)
% ==========================
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% ==========================
%   HYPERREF
% ==========================
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue,
  citecolor=black,
  pdfauthor={$if(author)$$author$$endif$},
  pdftitle={$if(title)$$title$$endif$}
}

% ==========================
%   HEADER / FOOTER
% ==========================
\usepackage{fancyhdr}

\pagestyle{fancy}
\fancyhf{}

% Header text only, no background color
\lhead{\small $if(student-name)$$student-name$$endif$ \; | \; $if(academic-year)$$academic-year$$endif$}
\rhead{}
\cfoot{\thepage}

\renewcommand{\headrulewidth}{0.4pt} % Thin separator line

% ==========================
%   COVER PAGE STYLE
% ==========================
\fancypagestyle{coverpage}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}

% ==========================
%   DOCUMENT START
% ==========================
\begin{document}

% ==========================
%   COVER PAGE
% ==========================
$if(coverpage)$
\thispagestyle{coverpage}
\begin{center}

% Logo at top
$if(logo)$
\includegraphics[width=0.5\textwidth]{$logo$}
\vspace{1cm}
$endif$

% Student name and academic year at top
{\large 
$if(student-name)$
\textbf{$student-name$} \\
$endif$
$if(academic-year)$
$academic-year$ \\
$endif$
}

\vspace{3cm}

% Title
$if(title)$
{\Huge \bfseries $title$ \par}
\vspace{2cm}
$endif$

% Master degree information
$if(master-degree)$
{\Large $master-degree$ \par}
\vspace{0.5cm}
$endif$

$if(master-year)$
{\large $master-year$ \par}
$endif$

\vfill

% Date at bottom
$if(date)$
{\large $date$ \par}
$endif$

\end{center}
\clearpage
$endif$

% ==========================
%   TABLE OF CONTENTS (Page 2)
% ==========================
$if(toc)$
{
\hypersetup{linkcolor=black}
\renewcommand{\contentsname}{Table des Matières} % Pour l'avoir en français
\tableofcontents
}
\clearpage % Fin de la page 2, le contenu commencera en page 3
$endif$

% ==========================
%   MAIN CONTENT
% ==========================
$body$

\end{document}
